language: cpp

dist: xenial 

script:
  - mkdir -p build 
  - cd build
  - cmake .. -DRUN_UNIT_TEST=ON
  - cmake --build . 

matrix:
  include:
    - compiler: gcc
    - compiler: clang 

notifications:
  email:
    on_success: change
    on_failure: always
